require("dotenv").config();

const path = require("path");

module.exports = {
  config: path.resolve("db-ops", "config.js"),
  "models-path": path.resolve("src", "db", "models"),
  "seeders-path": path.resolve("db-ops", "seeders"),
  "migrations-path": path.resolve("db-ops", "migrations"),
};
